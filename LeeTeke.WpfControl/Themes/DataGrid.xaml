<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:leedep="clr-namespace:LeeTeke.WpfControl.Dependencies"
                    xmlns:controls="clr-namespace:LeeTeke.WpfControl.Controls"
                    xmlns:conver="clr-namespace:LeeTeke.WpfControl.Converters"
                    xmlns:local="clr-namespace:LeeTeke.WpfControl.Themes">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/LeeTeke.WpfControl;component/Converters.xaml" />
        <ResourceDictionary  Source="pack://application:,,,/LeeTeke.WpfControl;component/Themes/CheckBox.xaml" />
        <ResourceDictionary  Source="pack://application:,,,/LeeTeke.WpfControl;component/Themes/ComboBox.xaml" />
        <ResourceDictionary  Source="pack://application:,,,/LeeTeke.WpfControl;component/Themes/TextBox.xaml" />
    </ResourceDictionary.MergedDictionaries>



    <Style x:Key="LeeDataGrid" TargetType="{x:Type DataGrid}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource LeeFocusVisual}"/>
        <Setter Property="SnapsToDevicePixels"  Value="{DynamicResource LeeSnapsToDevicePixels}" />
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="leedep:DataGridManager.DataGridHook" Value="True" />
        <Setter Property="SnapsToDevicePixels"  Value="True" />
        <Setter Property="FontSize"   Value="13" />
        <Setter Property="Foreground" Value="{DynamicResource LeeBrush_Text}"/>
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="RowHeaderWidth" Value="50"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource LeeBrush_BorderBrush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource LeeBrush_BorderBrush}"/>
        <Setter Property="leedep:DataGridManager.RowMargin" Value="0,2"/>
        <Setter Property="leedep:DataGridManager.RowCornerRadius" Value="{DynamicResource LeeCornerRadius}"/>
        <Setter Property="leedep:DataGridManager.RowIsClip" Value="True"/>
        <Setter Property="leedep:DataGridManager.RowBackground" Value="{DynamicResource LeeBrush_Forground}"/>
        <Setter Property="leedep:DataGridManager.HeaderFreeze" Value="True"/>
        <Setter Property="leedep:DataGridManager.RowHeaderVerticalContentAlignment" Value="Center"/>
        <Setter Property="leedep:DataGridManager.RowHeaderHorizontalContentAlignment" Value="Center"/>
        <Setter Property="leedep:DataGridManager.CellPadding"  Value="10,0" />
        <Setter Property="leedep:DataGridManager.RowMinHeight"   Value="35" />
        <Setter Property="leedep:DataGridManager.HeaderMinHeight"   Value="40" />
        <Setter Property="leedep:DataGridManager.ColumnVerticalContentAlignment"   Value="Center" />
        <Setter Property="leedep:DataGridManager.ColumnHorizontalContentAlignment"    Value="Center" />
        <Setter Property="leedep:DataGridManager.SelectedBackground"    Value="{DynamicResource LeeBrush_Theme}" />
        <Setter Property="leedep:DataGridManager.SelectedForeground"    Value="Red" />
        <Setter Property="leedep:DataGridManager.MouseOrverForeground"    Value="{x:Null}" />
        <Setter Property="leedep:DataGridManager.MouseOverBackground"    Value="{DynamicResource LeeBrush_Mark}" />
        <Setter Property="leedep:DataGridManager.HeaderBackground"   Value="Transparent" />
        <Setter Property="leedep:DataGridManager.HeaderForeground"  Value="{DynamicResource LeeBrush_Text}" />
        <Setter Property="leedep:DataGridManager.AutoGenerateComboxStyle" >
            <Setter.Value>
                <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource  LeeComboBox}">
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="leedep:DataGridManager.AutoGenerateCheckBoxStyle" >
            <Setter.Value>
                <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource  SwitchCheckBox}">
                    <Setter Property="IsEnabled" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}},Path=IsReadOnly,Mode=OneWay}"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="leedep:DataGridManager.AutoGenerateImageStyle" >
            <Setter.Value>
                <Style TargetType="{x:Type Image}" >
                    <Setter Property="MaxWidth" Value="100"/>
                    <Setter Property="MaxHeight" Value="100"/>
                    <Setter Property="HorizontalAlignment" Value="Stretch"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="leedep:DataGridManager.AutoGenerateTextBoxStyle" >
            <Setter.Value>
                <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource  LeeTextBox}">
                    <Setter Property="Background" Value="{DynamicResource LeeBrush_White}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </Setter.Value>
        </Setter>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="Foreground"  Value="{DynamicResource LeeBrush_Text}" />
        <Setter Property="RowValidationErrorTemplate">
            <Setter.Value>
                <ControlTemplate>
                    <TextBlock Text=" &#xEB90;"
                                   FontSize="{TemplateBinding FontSize}"
                                   FontWeight="Bold"
                                   FontFamily="{DynamicResource IconFontFamily}"
                                   Foreground="{DynamicResource LeeBrush_Error}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   ToolTip="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type DataGridRow}}, Path=(Validation.Errors)[0].ErrorContent}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>

            <Trigger Property="leedep:DataGridManager.HeaderFreeze" Value="True">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate  TargetType="{x:Type DataGrid}">
                            <Grid Margin="{TemplateBinding Margin}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition/>
                                </Grid.RowDefinitions>
                                <Border x:Name="PART_HeaderBorder"
                                        Grid.ColumnSpan="2"
                                        Background="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(leedep:DataGridManager.HeaderBackground),Mode=OneWay}"
                                    BorderBrush="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(leedep:DataGridManager.HeaderBorderBursh),Mode=OneWay}"
                                     BorderThickness="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(leedep:DataGridManager.HeaderBorderThickness),Mode=OneWay}"
                                     CornerRadius="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(leedep:DataGridManager.HeaderCornerRadius),Mode=OneWay}"
                                      Margin="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(leedep:DataGridManager.HeaderMargin),Mode=OneWay}"
                                      Padding="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(leedep:DataGridManager.HeaderPadding),Mode=OneWay}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Border BorderThickness="0"
                                                Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                            <CheckBox x:Name="PART_CheckBox"
                                                      leedep:DataGridManager.CheckBoxHook="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                      Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.All}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                      FontFamily="{DynamicResource IconFontFamily}"
                                                      FontSize="17"   
                                                      FontWeight="Bold"
                                                      HorizontalAlignment="Center"
                                                      />
                                        </Border>
                                        <DataGridColumnHeadersPresenter x:Name="PART_Header"
                                                                        Grid.Column="1"
                                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                        Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                    </Grid>
                                </Border>
                                <ScrollViewer Grid.Row="1" 
                                              Grid.ColumnSpan="2"
                                              x:Name="PART_ScrollViewer" 
                                              HorizontalScrollBarVisibility="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.HorizontalScrollBarVisibility),Mode=OneWay}" 
                                              VerticalScrollBarVisibility="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.VerticalScrollBarVisibility),Mode=OneWay}"
                                              CanContentScroll="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.CanContentScroll),Mode=OneWay}"
                                              PanningMode="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.PanningMode),Mode=OneWay}"
                                              Background="{TemplateBinding Background}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              leedep:CornerRadiusManager.CornerRadius="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path= (leedep:CornerRadiusManager.CornerRadius),Mode=OneWay}"
                                              leedep:CornerRadiusManager.IsClip="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path= (leedep:CornerRadiusManager.IsClip),Mode=OneWay}"
                                              >
                                    <Grid Margin="{TemplateBinding Padding}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition Height="auto"/>
                                        </Grid.RowDefinitions>

                                        <!--背景-->
                                        <ContentControl Grid.Row="0"
                                                Content="{Binding Path=(leedep:DataGridManager.NoItemsContent),  RelativeSource={RelativeSource AncestorType={x:Type DataGrid},AncestorLevel=1},Mode=OneWay}"
                                                Visibility="{TemplateBinding  HasItems,Converter={StaticResource VisibilityForBoolReverseConverter}}"/>
                                        <ItemsPresenter   SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                        <!--尾部-->
                                        <ContentControl x:Name="PART_ListEnd" 
                                                Grid.Row="1" 
                                                Content="{Binding Path=(leedep:DataGridManager.ListEndContent),  RelativeSource={RelativeSource AncestorType={x:Type DataGrid},AncestorLevel=1},Mode=OneWay}"
                                                Visibility="{TemplateBinding  HasItems,Converter={StaticResource VisibilityForBoolConverter}}"/>

                                    </Grid>
                                </ScrollViewer>
                            </Grid>
                            <ControlTemplate.Triggers>

                                <Trigger Property="leedep:DataGridManager.HeaderIsClip" Value="True">
                                    <Setter TargetName="PART_Header" Property="Clip">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource MultiValueToClipConverter}" Mode="OneWay">
                                                <Binding RelativeSource="{RelativeSource Mode=Self}"   Path="ActualWidth" Mode="OneWay"/>
                                                <Binding RelativeSource="{RelativeSource Mode=Self}"   Path="ActualHeight" Mode="OneWay"/>
                                                <Binding ElementName="PART_HeaderBorder"  Path="CornerRadius" Mode="OneWay"/>
                                          
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>

                                <Trigger Property="SelectionMode" Value="Single">
                                    <Setter  TargetName="PART_CheckBox" Property="Visibility" Value="Collapsed"/>
                                </Trigger>

                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="leedep:DataGridManager.HeaderFreeze" Value="False">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate  TargetType="{x:Type DataGrid}">
                            <ScrollViewer  x:Name="PART_ScrollViewer" 
                                      HorizontalScrollBarVisibility="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.HorizontalScrollBarVisibility),Mode=OneWay}" 
                                      VerticalScrollBarVisibility="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.VerticalScrollBarVisibility),Mode=OneWay}"
                                      CanContentScroll="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.CanContentScroll),Mode=OneWay}"
                                      PanningMode="{Binding  RelativeSource={ RelativeSource  Mode=FindAncestor,AncestorType=DataGrid},Path=(ScrollViewer.PanningMode),Mode=OneWay}"
                                      Background="{TemplateBinding Background}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      Margin="{TemplateBinding Margin}"
                                      leedep:CornerRadiusManager.CornerRadius="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path= (leedep:CornerRadiusManager.CornerRadius),Mode=OneWay}"
                                      leedep:CornerRadiusManager.IsClip="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path= (leedep:CornerRadiusManager.IsClip),Mode=OneWay}"
                                      >
                                <Grid Margin="{TemplateBinding Padding}">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="auto"/>
                                        <RowDefinition/>
                                        <RowDefinition Height="auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="auto"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Border BorderThickness="0"
                                                Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                            <CheckBox x:Name="PART_CheckBox"
                                                Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.All}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                FontFamily="{DynamicResource IconFontFamily}"
                                                FontSize="17"
                                                FontWeight="Bold"
                                                HorizontalAlignment="Center"
                                                />
                                        </Border>
                                        <DataGridColumnHeadersPresenter x:Name="PART_Header"
                                                                        Grid.Column="1"
                                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                                        Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                                    </Grid>
                                    <!--背景-->
                                    <ContentControl Grid.Row="1"
                                                Content="{Binding Path=(leedep:DataGridManager.NoItemsContent),  RelativeSource={RelativeSource AncestorType={x:Type DataGrid},AncestorLevel=1},Mode=OneWay}"
                                                Visibility="{TemplateBinding  HasItems,Converter={StaticResource VisibilityForBoolReverseConverter}}"/>
                                    <ItemsPresenter Grid.Row="1"   
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                    <!--尾部-->
                                    <ContentControl x:Name="PART_ListEnd" 
                                                Grid.Row="2" 
                                                Content="{Binding Path=(leedep:DataGridManager.ListEndContent),  RelativeSource={RelativeSource AncestorType={x:Type DataGrid},AncestorLevel=1},Mode=OneWay}"
                                                Visibility="{TemplateBinding  HasItems,Converter={StaticResource VisibilityForBoolConverter}}"/>

                                </Grid>
                            </ScrollViewer>
                            <ControlTemplate.Triggers>

                                <Trigger Property="leedep:DataGridManager.HeaderIsClip" Value="True">
                                    <Setter TargetName="PART_Header" Property="Clip">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource MultiValueToClipConverter}" Mode="OneWay">
                                                <Binding RelativeSource="{RelativeSource Mode=Self}"   Path="ActualWidth" Mode="OneWay"/>
                                                <Binding RelativeSource="{RelativeSource Mode=Self}"   Path="ActualHeight" Mode="OneWay"/>
                                                <Binding ElementName="PART_HeaderBorder"  Path="CornerRadius" Mode="OneWay"/>
                                             
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="SelectionMode" Value="Single">
                                    <Setter  TargetName="PART_CheckBox" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>

            <Trigger Property="IsEnabled"
                     Value="False">
                <Setter Property="Opacity"
                        Value="0.4" />
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll"
                        Value="False" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LeeDataGridColumnHeader" TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource LeeFocusVisual}"/>
        <Setter Property="SnapsToDevicePixels"   Value="{DynamicResource LeeSnapsToDevicePixels}" />
        <Setter Property="FontSize" Value="{Binding FontSize,RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="Background"  Value="{Binding Path=(leedep:DataGridManager.HeaderBackground),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="Foreground"  Value="{DynamicResource LeeBrush_Theme}" />
        <Setter Property="MinHeight" Value="{Binding Path=(leedep:DataGridManager.HeaderMinHeight),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="VerticalContentAlignment"      Value="{Binding Path=(leedep:DataGridManager.ColumnVerticalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="HorizontalContentAlignment"   Value="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="leedep:DataGridManager.HeaderPadding"  Value="{Binding Path=(leedep:DataGridManager.HeaderPadding),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                            Background="{TemplateBinding Background}"
                            Margin="{TemplateBinding Margin}">

                        <Grid  >
                            <StackPanel Orientation="Horizontal"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        Margin="{TemplateBinding Padding}">
                                <ContentPresenter 
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                 HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                <ToggleButton x:Name="PART_Sort" 
                                          Grid.Column="1"
                                          IsHitTestVisible="False"
                                          Style="{DynamicResource RotateToggleButton}"
                                          Margin="2,0"/>
                            </StackPanel>
                            <Thumb x:Name="PART_LeftHeaderGripper"
                               HorizontalAlignment="Left"
                               Width="2"
                                Grid.ColumnSpan="3"
                               Cursor="SizeWE"/>
                            <Thumb x:Name="PART_RightHeaderGripper"
                                   Grid.ColumnSpan="3"
                               HorizontalAlignment="Right"
                               Width="2"
                               Cursor="SizeWE"/>
                        </Grid>


                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay,Converter={StaticResource  NotAlignmentCenterConverter}}"
                                           Value="False" />
                                <Condition Binding="{Binding SortDirection,RelativeSource={RelativeSource Self}}"
                                           Value="{x:Null}" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Sort"
                                    Property="Visibility"
                                    Value="Collapsed" />
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay,Converter={StaticResource NotAlignmentCenterConverter}}"
                                           Value="False" />
                                <Condition Binding="{Binding SortDirection,RelativeSource={RelativeSource Self}}"
                                           Value="Descending" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Sort"  Property="IsChecked" Value="False"/>

                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay,Converter={StaticResource NotAlignmentCenterConverter}}"
                                           Value="False" />
                                <Condition Binding="{Binding SortDirection,RelativeSource={RelativeSource Self}}"
                                           Value="Ascending" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Sort"  Property="IsChecked" Value="True"/>

                        </MultiDataTrigger>
                        <DataTrigger Binding="{Binding CanUserSortColumns,RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}"
                                     Value="False">
                            <Setter TargetName="PART_Sort"
                                    Property="Visibility"
                                    Value="Collapsed" />
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay,Converter={StaticResource NotAlignmentCenterConverter}}"
                                           Value="True" />
                                <Condition Binding="{Binding SortDirection,RelativeSource={RelativeSource Self}}"
                                           Value="{x:Null}" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Sort"  Property="Visibility" Value="Collapsed"/>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay,Converter={StaticResource NotAlignmentCenterConverter}}"
                                           Value="True" />
                                <Condition Binding="{Binding SortDirection,RelativeSource={RelativeSource Self}}"
                                           Value="Descending" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Sort"  Property="IsChecked" Value="False"/>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay,Converter={StaticResource NotAlignmentCenterConverter}}"
                                           Value="True" />
                                <Condition Binding="{Binding SortDirection,RelativeSource={RelativeSource Self}}"
                                           Value="Ascending" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Sort"  Property="IsChecked" Value="True"/>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="SortDirection" Value="{x:Null}">
                <Setter Property="Foreground" Value="{Binding Path=(leedep:DataGridManager.HeaderForeground),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="LeeDataGridCell" TargetType="{x:Type DataGridCell}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource LeeFocusVisual}"/>
        <Setter Property="SnapsToDevicePixels"   Value="{DynamicResource LeeSnapsToDevicePixels}" />
        <Setter Property="FontSize"  Value="{Binding FontSize,RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding"  Value="{Binding Path=(leedep:DataGridManager.CellPadding),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="VerticalContentAlignment"  Value="{Binding Path=(leedep:DataGridManager.ColumnVerticalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="HorizontalContentAlignment"  Value="{Binding Path=(leedep:DataGridManager.ColumnHorizontalContentAlignment),RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="leedep:DataGridManager.SelectedForeground" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.SelectedForeground),Mode=OneWay}"/>
        <Setter Property="leedep:DataGridManager.SelectedBackground" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.SelectedBackground),Mode=OneWay}"/>
        <Setter Property="leedep:DataGridManager.SelectedBorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.SelectedBorderBrush),Mode=OneWay}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Border x:Name="PART_Border"
                            BorderThickness="0"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter x:Name="Presenter"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected,RelativeSource={RelativeSource Self}}"  Value="True"/>
                                <Condition Binding="{Binding SelectionUnit,RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}" Value="CellOrRowHeader" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Border"  Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}},Path=(leedep:DataGridManager.SelectedBackground),Mode=OneWay}"/>
                            <Setter TargetName="PART_Border"  Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}},Path=(leedep:DataGridManager.SelectedBorderBrush),Mode=OneWay}"/>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected,RelativeSource={RelativeSource Self}}"  Value="True"/>
                                <Condition Binding="{Binding SelectionUnit,RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}" Value="Cell" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Border"  Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}},Path=(leedep:DataGridManager.SelectedBackground),Mode=OneWay}"/>
                            <Setter TargetName="PART_Border"  Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}},Path=(leedep:DataGridManager.SelectedBorderBrush),Mode=OneWay}"/>
                        </MultiDataTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected,RelativeSource={RelativeSource Self}}"  Value="True"/>
                                <Condition Binding="{Binding Path=(leedep:DataGridManager.SelectedForeground),RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}},Converter={StaticResource IsNotNullConverter}}"  Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Foreground"  Value="{Binding Path=(leedep:DataGridManager.SelectedForeground),RelativeSource={RelativeSource AncestorType={x:Type DataGridCell}}}" />
                        </MultiDataTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LeeDataGridRow" TargetType="{x:Type DataGridRow}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource LeeFocusVisual}"/>
        <Setter Property="SnapsToDevicePixels"   Value="{DynamicResource LeeSnapsToDevicePixels}" />
        <Setter Property="FontSize"   Value="{Binding FontSize,RelativeSource={RelativeSource AncestorType=DataGrid}}" />
        <Setter Property="MinHeight" Value="{Binding Path=(leedep:DataGridManager.RowMinHeight),RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}" />
        <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowBackground),Mode=OneWay}"/>
        <Setter Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowBorderBrush),Mode=OneWay}"/>
        <Setter Property="BorderThickness" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowBorderThickness),Mode=OneWay}"/>
        <Setter Property="Margin" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowMargin),Mode=OneWay}"/>
        <Setter Property="Padding" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowPadding),Mode=OneWay}"/>
        <Setter Property="leedep:CornerRadiusManager.IsClip" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowIsClip),Mode=OneWay}"/>
        <Setter Property="leedep:CornerRadiusManager.CornerRadius" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowCornerRadius),Mode=OneWay}"/>
        <Setter Property="leedep:DataGridManager.MouseOrverBorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.MouseOverBackground),Mode=OneWay}"/>
        <Setter Property="leedep:DataGridManager.MouseOverBackground" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.MouseOverBackground),Mode=OneWay}"/>
        <Setter Property="leedep:DataGridManager.SelectedBackground" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.SelectedBackground),Mode=OneWay}"/>
        <Setter Property="leedep:DataGridManager.SelectedBorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.SelectedBorderBrush),Mode=OneWay}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRow}">
                    <Border x:Name="PART_Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Margin="{TemplateBinding Margin}"
                            CornerRadius="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}},Path=(leedep:CornerRadiusManager.CornerRadius),Mode=OneWay}">
                        <Grid x:Name="PART_Grid" 
                             Margin="{TemplateBinding Padding}">
                            <SelectiveScrollingGrid>
                                <SelectiveScrollingGrid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </SelectiveScrollingGrid.ColumnDefinitions>
                                <SelectiveScrollingGrid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </SelectiveScrollingGrid.RowDefinitions>
                                <DataGridCellsPresenter Grid.Column="1"
                                                    ItemsPanel="{TemplateBinding ItemsPanel}"
                                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                <DataGridDetailsPresenter Grid.Column="1"
                                                      Grid.Row="1"
                                                      Visibility="{TemplateBinding DetailsVisibility}"
                                                      SelectiveScrollingGrid.SelectiveScrollingOrientation="{Binding AreRowDetailsFrozen,  ConverterParameter={x:Static SelectiveScrollingOrientation.Vertical}, Converter={x:Static DataGrid.RowDetailsScrollingConverter},  RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                                <DataGridRowHeader Grid.RowSpan="2"
                                               SelectiveScrollingGrid.SelectiveScrollingOrientation="Vertical"
                                               Visibility="{Binding HeadersVisibility,  ConverterParameter={x:Static DataGridHeadersVisibility.Row},  Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" />
                            </SelectiveScrollingGrid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                   
                        
                        <Trigger Property="leedep:CornerRadiusManager.IsClip" Value="True">
                            <Setter TargetName="PART_Grid" Property="Clip">
                                <Setter.Value>
                                    <MultiBinding Converter="{StaticResource MultiValueToClipConverter}" Mode="OneWay">
                                        <Binding RelativeSource="{RelativeSource Mode=Self}"   Path="ActualWidth" Mode="OneWay"/>
                                        <Binding RelativeSource="{RelativeSource Mode=Self}"   Path="ActualHeight" Mode="OneWay"/>
                                        <Binding ElementName="PART_Border"  Path="CornerRadius" Mode="OneWay"/>
                              
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsMouseOver,RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding SelectionUnit,RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}" Value="FullRow" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Border"  Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}},Path=(leedep:DataGridManager.MouseOverBackground),Mode=OneWay}"/>
                            <Setter TargetName="PART_Border"  Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}},Path=(leedep:DataGridManager.MouseOrverBorderBrush),Mode=OneWay}"/>
                        </MultiDataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsSelected,RelativeSource={RelativeSource Self}}" Value="True" />
                                <Condition Binding="{Binding SelectionUnit,RelativeSource={RelativeSource AncestorType=DataGrid},Mode=OneWay}" Value="FullRow" />
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="PART_Border"  Property="Background" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}},Path=(leedep:DataGridManager.SelectedBackground),Mode=OneWay}"/>
                            <Setter TargetName="PART_Border"  Property="BorderBrush" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}},Path=(leedep:DataGridManager.SelectedBorderBrush),Mode=OneWay}"/>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="LeeDataGridRowHeader" TargetType="{x:Type DataGridRowHeader}">
        <Setter Property="FocusVisualStyle" Value="{DynamicResource LeeFocusVisual}"/>
        <Setter Property="SnapsToDevicePixels"  Value="{DynamicResource LeeSnapsToDevicePixels}" />
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalContentAlignment" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowHeaderVerticalContentAlignment),Mode=OneWay}"/>
        <Setter Property="HorizontalContentAlignment" Value="{Binding RelativeSource={RelativeSource AncestorType={x:Type DataGrid}},Path=(leedep:DataGridManager.RowHeaderHorizontalContentAlignment),Mode=OneWay}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridRowHeader}">
                    <Grid Margin="{TemplateBinding Margin}" >
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" >
                            <ContentPresenter RecognizesAccessKey="True"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  />
                        </Border>
                        <!--<Control SnapsToDevicePixels="false"
                                     VerticalAlignment="Center"
                                     HorizontalAlignment="Center"
                                     Template="{Binding ValidationErrorTemplate, RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}}}"
                                     Visibility="{Binding (Validation.HasError), Converter={StaticResource VisibilityForBoolConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGridRow}}}" />-->

                        <Thumb x:Name="PART_TopHeaderGripper"
                               VerticalAlignment="Top"
                               Height="2"
                               Cursor="SizeNS"/>

                        <Thumb x:Name="PART_BottomHeaderGripper"
                               VerticalAlignment="Bottom"
                               Height="2"
                               Cursor="SizeNS"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>